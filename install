git submodule init
git submodule update --remote --recursive
cd libmain/fish_clock_xs
perl Makefile.PL && make 

# FIXME, this sucks
sudo mkdir /usr/share/fish-clock -p
sudo cp conf/config-base /usr/share/fish-clock/config
cp -f conf/config ~/.fish-clock-config
